# Streamlit for building the web app
streamlit>=1.16.0

# NumPy for numerical computations (latest compatible version)
numpy>=1.25.0

# Other dependencies
pandas>=1.5.0
scipy>=1.9.0
scikit-learn>=1.0.0

# Use setuptools instead of distutils (if needed for building extensions)
setuptools>=67.0
